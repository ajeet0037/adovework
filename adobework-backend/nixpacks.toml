[phases.setup]
nixPkgs = ["python311", "tesseract", "poppler_utils", "qpdf", "ffmpeg", "libGL"]

[phases.build]
cmds = ["python -m venv venv", ". venv/bin/activate", "pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
